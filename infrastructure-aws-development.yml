instance_groups:
- name: etcd
  jobs:
  - name: cron
    properties:
      cron:
        variables:
          ENVIRONMENT: development
- name: master
  jobs:
  - name: kubernetes-minion
    properties:
      aws:
        cluster-tag: kubernetes-development
- name: minion
  vm_type: r5.large
  jobs:
  - name: kubernetes-minion
    properties:
      aws:
        cluster-tag: kubernetes-development
