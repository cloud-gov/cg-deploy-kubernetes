instance_groups:
- name: master
  jobs:
  - name: kubernetes-minion
    properties:
      aws:
        cluster-tag: kubernetes-production
- name: minion
  jobs:
  - name: kubernetes-minion
    properties:
      aws:
        cluster-tag: kubernetes-production
